import { FormEvent, useCallback, useEffect, useRef, useState } from 'react'
import { Bell, Camera, Check, CheckCheck, ChevronLeft, Heart, LogOut, MessageCircleMore, Send, Settings, ShieldCheck, Trash2, UsersRound, Video, X } from 'lucide-react'
import { supabase } from './supabase'
import './community-lobby.css'
import { useFocusTrap } from './hooks/useFocusTrap'

export type DbRoom = { id:string; name:string; description:string; icon:string; theme:string; is_private:boolean; tags?:string[]; total_members?:number; online_members?:number }
type Profile = {
  id:string; full_name:string; display_name:string|null; avatar_url:string|null; country:string|null; city?:string|null;
  profile_type:string; professional_verification_status?:string|null; professional_title?:string|null; years_experience?:number|null;
  availability?:string|null; about:string; interests:string[]; specialties:string[]; languages?:string[]; online:boolean;
  visibility?:string|null; discoverable?:boolean|null; birth_date?:string|null; birth_date_visibility?:string|null; pronouns?:string|null;
  personal_website?:string|null; professional_website?:string|null; linkedin_url?:string|null;
  location?:string|null; certifications?:string[]|null; session_price?:number|null;
  online_available?:boolean|null; in_person_available?:boolean|null;
  social_instagram_url?:string|null; social_facebook_url?:string|null; social_youtube_url?:string|null;
  cover_image_url?:string|null;
}
type Message = { id:string; body:string; sender_id:string; created_at:string; edited_at?:string|null; updated_at?:string|null; read_at?:string|null; reply_to?:string|null; pinned?:boolean; media_url?:string|null; media_type?:string|null; media_mime_type?:string|null; media_size?:number|null; profiles?:{full_name:string;avatar_url:string|null}|null }
type Reaction = { message_id:string; emoji:string; user_id:string }
type Friendship = { id:string; requester_id:string; addressee_id:string; status:string }
type Notice = { id:string; type:string; title:string; body:string|null; entity_id:string|null; read_at:string|null; created_at:string }

const QUICK_REACTIONS = ['❤️','🙏','🌿','✨']
const initials = (name?:string|null) => (name || 'N').split(' ').map(x=>x[0]).join('').slice(0,2)
const accountTypeLabel = (value?:string|null) => value === 'healer' ? 'Healer' : value === 'admin' ? 'Administrator' : 'Member'
const isApprovedHealer = (profile?:Profile|null) => Boolean(profile && profile.profile_type === 'healer')
const splitList = (value:FormDataEntryValue|null) => String(value||'').split(',').map(x=>x.trim()).filter(Boolean)
const safeUrl = (value:FormDataEntryValue|null) => {
  const url = String(value||'').trim()
  if (!url) return null
  return /^https?:\/\//i.test(url) ? url : `https://${url}`
}
const connectionFor = (id:string, rows:Friendship[]) => rows.find(f=>f.requester_id===id||f.addressee_id===id)
function stateFor(userId:string, id:string, rows:Friendship[], blocked:string[]) {
  if(blocked.includes(id)) return 'blocked'
  const row=connectionFor(id,rows)
  if(!row) return 'none'
  if(row.status==='accepted') return 'accepted'
  if(row.status==='pending'&&row.requester_id===userId) return 'sent'
  if(row.status==='pending'&&row.addressee_id===userId) return 'incoming'
  return 'none'
}
function spam(body:string){const t=body.toLowerCase();return (t.match(/https?:\/\//g)||[]).length>2||/(.)\1{18,}/.test(t)||/(free money|crypto giveaway|click here now|earn \$|telegram.me|whatsapp group)/i.test(t)}

function ConnectButton({userId,person,friendships,blocked,onChanged}:{userId:string;person:Profile;friendships:Friendship[];blocked:string[];onChanged:()=>void}) {
  const [busy,setBusy]=useState(false),[error,setError]=useState('')
  const row=connectionFor(person.id,friendships), state=stateFor(userId,person.id,friendships,blocked)
  async function run(call:()=>any){setBusy(true);setError('');const result=await call();setBusy(false);if(result?.error)setError(result.error.message||'Connect action failed.');else onChanged()}
  if(person.id===userId)return <button disabled>Connect unavailable</button>
  if(state==='blocked')return <button disabled>Blocked</button>
  return <span className="connect-control">
    {state==='none'&&<button disabled={busy} onClick={()=>run(()=>supabase.rpc('send_connection_request',{other_user:person.id}))}>{busy?'Sending…':'Connect'}</button>}
    {state==='sent'&&<button disabled={busy||!row} onClick={()=>row&&run(()=>supabase.rpc('cancel_connection_request',{request_id:row.id}))}>Request sent</button>}
    {state==='incoming'&&<><button disabled={busy||!row} onClick={()=>row&&run(()=>supabase.rpc('respond_connection_request',{request_id:row.id,next_status:'accepted'}))}>Accept connection</button><button disabled={busy||!row} onClick={()=>row&&run(()=>supabase.rpc('respond_connection_request',{request_id:row.id,next_status:'declined'}))}>Decline</button></>}
    {state==='accepted'&&<button disabled={busy||!row} onClick={()=>row&&window.confirm('Remove this connection?')&&run(()=>supabase.rpc('remove_connection',{request_id:row.id}))}>Connected</button>}
    {error&&<small>{error}</small>}
  </span>
}

export function ChatRoom({ room, userId, onClose }:{room:DbRoom;userId:string;onClose:()=>void}) {
  const [messages,setMessages]=useState<Message[]>([]),[reactions,setReactions]=useState<Reaction[]>([]),[text,setText]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(true)
  const [replyTo,setReplyTo]=useState<Message|null>(null),[editing,setEditing]=useState<Message|null>(null),[editText,setEditText]=useState(''),[muted,setMuted]=useState<string[]>([]),[typing,setTyping]=useState<string[]>([]),[unread,setUnread]=useState(0)
  const bottom=useRef<HTMLDivElement>(null),listRef=useRef<HTMLDivElement>(null),channelRef=useRef<any>(null),typingTimer=useRef<number|null>(null)
  const containerRef = useFocusTrap(true)
  const nearBottom=()=>{const el=listRef.current;return !el||el.scrollHeight-el.scrollTop-el.clientHeight<80}
  const markRead=useCallback(async()=>{if(room.is_private)await supabase.rpc('mark_room_read',{target_room:room.id})},[room.id,room.is_private])
  const load=useCallback(async(show=false)=>{if(show)setLoading(true);const {data,error}=await supabase.from('messages').select('id,body,sender_id,created_at,edited_at,updated_at,read_at,reply_to,pinned,media_url,media_type,media_mime_type,media_size,profiles!messages_sender_id_fkey(full_name,avatar_url)').eq('room_id',room.id).is('deleted_at',null).order('created_at').range(0,199);if(error){setError(error.message);setLoading(false);return}const rows=(data as unknown as Message[])||[];setMessages(rows);if(rows.length){const {data:r}=await supabase.from('message_reactions').select('message_id,emoji,user_id').in('message_id',rows.map(m=>m.id));setReactions((r as Reaction[])||[])}else setReactions([]);setLoading(false);markRead()},[room.id,markRead])
  useEffect(()=>{setMessages([]);setUnread(0);load(true);const c=supabase.channel(`room-${room.id}`,{config:{presence:{key:userId}}}).on('postgres_changes',{event:'INSERT',schema:'public',table:'messages',filter:`room_id=eq.${room.id}`},payload=>{const next=payload.new as Message;if(next.sender_id!==userId&&!nearBottom())setUnread(x=>x+1);load()}).on('postgres_changes',{event:'UPDATE',schema:'public',table:'messages',filter:`room_id=eq.${room.id}`},()=>load()).on('postgres_changes',{event:'*',schema:'public',table:'message_reactions'},()=>load()).on('presence',{event:'sync'},()=>{const s=c.presenceState() as Record<string,any[]>;setTyping(Object.entries(s).filter(([id])=>id!==userId).flatMap(([,v])=>v.filter(m=>m.typing).map(m=>m.name||'Someone')))}).subscribe(async status=>{if(status==='SUBSCRIBED')await c.track({typing:false,name:'Member'})});channelRef.current=c;return()=>{supabase.removeChannel(c)}},[room.id,userId,load])
  useEffect(()=>{if(nearBottom()){bottom.current?.scrollIntoView({behavior:'smooth'});setUnread(0)}},[messages])
  function updateText(v:string){setText(v);channelRef.current?.track({typing:Boolean(v.trim()),name:'Member'});if(typingTimer.current)window.clearTimeout(typingTimer.current);typingTimer.current=window.setTimeout(()=>channelRef.current?.track({typing:false,name:'Member'}),1200)}
  const counts=(id:string,emoji:string)=>({count:reactions.filter(r=>r.message_id===id&&r.emoji===emoji).length,me:reactions.some(r=>r.message_id===id&&r.emoji===emoji&&r.user_id===userId)})
  async function send(e:FormEvent){e.preventDefault();const body=text.trim();if(!body)return;if(spam(body)){setError('This looks like spam. Please rewrite it in a calmer, more personal way.');return}setText('');const optimistic={id:`local-${Date.now()}`,room_id:room.id,sender_id:userId,body,created_at:new Date().toISOString(),reply_to:replyTo?.id||null,deleted_at:null} as unknown as Message;setMessages(x=>[...x,optimistic]);const {error}=await supabase.from('messages').insert({room_id:room.id,sender_id:userId,body,reply_to:replyTo?.id||null});if(error){setError(error.message);setText(body);setMessages(x=>x.filter(m=>m.id!==optimistic.id))}else setReplyTo(null)}
  async function saveEdit(e:FormEvent){e.preventDefault();if(!editing)return;const body=editText.trim();if(!body||spam(body))return;const {error}=await supabase.from('messages').update({body,edited_at:new Date().toISOString()}).eq('id',editing.id).eq('sender_id',userId);if(error)setError(error.message);else{setEditing(null);load()}}
  async function remove(m:Message){const {error}=await supabase.from('messages').update({deleted_at:new Date().toISOString(),body:'Message deleted'}).eq('id',m.id).eq('sender_id',userId);if(error)setError(error.message);else load()}
  async function react(m:Message,emoji:string){await supabase.from('message_reactions').upsert({message_id:m.id,user_id:userId,emoji},{onConflict:'message_id,user_id,emoji'});load()}
  async function report(m:Message){const reason=window.prompt('Tell the moderators what felt unsafe about this message.');if(reason)await supabase.from('message_reports').insert({message_id:m.id,reporter_id:userId,reason})}
  async function leave(){if(room.is_private){onClose();return}await supabase.from('room_members').delete().eq('room_id',room.id).eq('user_id',userId);onClose()}
  async function uploadImage(){
    const input=document.createElement('input')
    input.type='file'
    input.accept='image/jpeg,image/png,image/gif,image/webp'
    input.onchange=async()=>{
      const file=input.files?.[0]
      if(!file)return
      const path=`${userId}/${room.id}/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'-')}`
      const {error:uploadErr}=await supabase.storage.from('chat-media').upload(path,file,{contentType:file.type,upsert:false})
      if(uploadErr){setError(uploadErr.message);return}
      const {data:urlData}=supabase.storage.from('chat-media').getPublicUrl(path)
      const mediaUrl=urlData?.publicUrl
      if(!mediaUrl){setError('Could not get image URL.');return}
      const {error:msgErr}=await supabase.from('messages').insert({room_id:room.id,sender_id:userId,body:'',media_url:mediaUrl,media_type:'image',media_mime_type:file.type,media_size:file.size})
      if(msgErr)setError(msgErr.message);else load()
    }
    input.click()
  }
  function openRoomCall(){
    window.open(`https://meet.jit.si/nova-room-${room.id}`,'_blank','noopener,noreferrer')
  }
  const visible=messages.filter(m=>!muted.includes(m.sender_id)),pinned=messages.find(m=>m.pinned)
  return <div className="feature-overlay" ref={containerRef}><section className="chat-window" role="dialog" aria-modal="true" aria-label="Chat room"><header><button onClick={onClose}><ChevronLeft/></button><div className={`chat-room-icon ${room.theme}`}>{room.icon}</div><div><h2>{room.name}</h2><span><i/> {room.is_private?'Private room for 2 users':`${room.online_members||0} online · ${room.total_members||0} members`} · Be kind</span></div><div className="chat-header-actions"><button onClick={openRoomCall} title="Open video call"><Video size={17}/></button><button onClick={uploadImage} title="Share an image"><Camera size={17}/></button><button onClick={onClose}><X/></button></div></header><div className="safety-strip"><button onClick={()=>alert('Community guidelines: be respectful, protect privacy, no harassment, no spam, and report harmful behavior.')}>Community Guidelines</button><button onClick={leave}>{room.is_private?'Close room':'Leave room'}</button><span>{room.is_private?'Only you and the other selected member can open this room.':'This is a peer-support space. Protect your privacy and report harmful behaviour.'}</span></div>{pinned&&<div className="pinned-message"><b>Moderator announcement</b><span>{pinned.body}</span></div>}<div className="message-list" ref={listRef} onScroll={()=>{if(nearBottom()){setUnread(0);markRead()}}}>{loading?<p className="empty-state">Opening the room…</p>:error?<div className="empty-state"><MessageCircleMore/><h3>Room unavailable</h3><p>{error}</p></div>:visible.length===0?<div className="empty-state"><MessageCircleMore/><h3>Start the conversation</h3><p>Be the first to share something kind or meaningful.</p></div>:visible.map((m,i)=>{const reply=messages.find(x=>x.id===m.reply_to),previous=visible[i-1],showDate=!previous||new Date(previous.created_at).toDateString()!==new Date(m.created_at).toDateString();return <div key={m.id}>{showDate&&<div className="date-separator">{new Date(m.created_at).toLocaleDateString([], {dateStyle:'medium'})}</div>}<div className={m.sender_id===userId?'chat-message mine':'chat-message'}><span>{m.profiles?.avatar_url?<img src={m.profiles.avatar_url} alt={(m.profiles?.full_name || 'Member') + " avatar"}/>:initials(m.profiles?.full_name)}</span><div>{reply&&<em className="reply-preview">Replying to {reply.sender_id===userId?'you':reply.profiles?.full_name||'member'}: {reply.body.slice(0,80)}</em>}<b>{m.sender_id===userId?'You':m.profiles?.full_name||'Member'} <small>{new Date(m.created_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}{m.edited_at?' · edited':''}{room.is_private&&m.sender_id===userId&&m.read_at?' · read':''}</small></b>{m.media_url&&m.media_type==='image'&&<div className="media-preview"><img src={m.media_url} alt={m.body||'Shared image'} loading="lazy" onClick={()=>window.open(m.media_url!,'_blank')}/></div>}{m.media_url&&m.media_type==='audio'&&<div className="media-preview"><audio controls preload="metadata" src={m.media_url}/></div>}{m.media_url&&m.media_type==='video'&&<div className="media-preview"><video controls preload="metadata" src={m.media_url} style={{maxWidth:'100%',maxHeight:300,borderRadius:8}}/></div>}<p>{m.body}</p><div className="message-actions">{QUICK_REACTIONS.map(e=>{const c=counts(m.id,e);return <button key={e} className={c.me?'active':''} onClick={()=>react(m,e)}>{e} {c.count||''}</button>})}<button onClick={()=>setReplyTo(m)}>Reply</button><button onClick={()=>navigator.clipboard?.writeText(m.body)}>Copy</button>{m.sender_id===userId&&<button onClick={()=>{setEditing(m);setEditText(m.body)}}>Edit</button>}{m.sender_id===userId&&<button onClick={()=>remove(m)}>Delete</button>}{m.sender_id!==userId&&<button onClick={()=>report(m)}>Report</button>}{m.sender_id!==userId&&<button onClick={()=>setMuted(x=>[...new Set([...x,m.sender_id])])}>Mute</button>}</div></div></div></div>})}<div ref={bottom}/></div>{unread>0&&<button className="unread-pill" onClick={()=>{bottom.current?.scrollIntoView({behavior:'smooth'});setUnread(0);markRead()}}>{unread} new</button>}{typing.length>0&&<div className="typing-indicator">{typing.slice(0,2).join(', ')} typing…</div>}{replyTo&&<div className="compose-context">Replying to {replyTo.sender_id===userId?'your message':replyTo.profiles?.full_name||'member'} <button onClick={()=>setReplyTo(null)}>Cancel</button></div>}{editing&&<form className="message-compose edit-compose" onSubmit={saveEdit}><input value={editText} onChange={e=>setEditText(e.target.value)} maxLength={4000} autoFocus aria-label="Edit message"/><button aria-label="Save"><Check/></button><button type="button" onClick={()=>setEditing(null)} aria-label="Cancel"><X/></button></form>}<form className="message-compose" onSubmit={send}><input value={text} onChange={e=>updateText(e.target.value)} maxLength={4000} placeholder="Write a supportive message…" aria-label="Write a message"/><button aria-label="Send"><Send/></button></form></section></div>
}

function useConnections(userId:string){
  const [friendships,setFriendships]=useState<Friendship[]>([]),[blocked,setBlocked]=useState<string[]>([])
  const load=useCallback(async()=>{const [{data:f},{data:b}]=await Promise.all([supabase.from('friendships').select('id,requester_id,addressee_id,status').or(`requester_id.eq.${userId},addressee_id.eq.${userId}`).in('status',['pending','accepted']),supabase.from('user_blocks').select('blocked_id').eq('blocker_id',userId)]);setFriendships((f as Friendship[])||[]);setBlocked(((b as {blocked_id:string}[])||[]).map(x=>x.blocked_id))},[userId])
  useEffect(()=>{load();const c=supabase.channel(`connections-hook-${userId}`).on('postgres_changes',{event:'*',schema:'public',table:'friendships'},()=>load()).on('postgres_changes',{event:'*',schema:'public',table:'user_blocks'},()=>load()).subscribe();return()=>{supabase.removeChannel(c)}},[userId,load])
  return {friendships,blocked,load}
}

export function PeopleDirectory({userId,onClose,onOpenRoom}:{userId:string;onClose:()=>void;onOpenRoom:(room:DbRoom)=>void}){
  const [people,setPeople]=useState<Profile[]>([]),[rooms,setRooms]=useState<DbRoom[]>([]),[query,setQuery]=useState(''),[joining,setJoining]=useState('')
  const containerRef = useFocusTrap(true)
  const {friendships,blocked,load}=useConnections(userId)
  useEffect(()=>{supabase.rpc('list_public_rooms').then(({data})=>setRooms((data as DbRoom[])||[]));supabase.from('profiles').select('id,full_name,display_name,avatar_url,country,profile_type,about,interests,specialties,online').neq('id',userId).limit(80).then(({data})=>setPeople((data as Profile[])||[]))},[userId])
  async function joinRoom(room:DbRoom){setJoining(room.id);const {error}=await supabase.from('room_members').insert({room_id:room.id,user_id:userId});if(error&&error.code!=='23505'){alert(error.message);setJoining('');return}await supabase.from('room_user_preferences').upsert({room_id:room.id,user_id:userId,last_read_at:new Date().toISOString()},{onConflict:'room_id,user_id'});setJoining('');onClose();onOpenRoom(room)}
  async function message(p:Profile){const {data,error}=await supabase.rpc('create_private_room',{other_user:p.id});if(error){alert(error.message);return}onClose();onOpenRoom({id:data,name:p.display_name||p.full_name,description:'Private two-person conversation',icon:'♢',theme:'sage',is_private:true})}
  async function video(p:Profile){const {data,error}=await supabase.from('video_sessions').insert({host_id:userId,guest_id:p.id}).select('id').single();if(!error&&data){await supabase.from('notifications').insert({user_id:p.id,actor_id:userId,type:'video_invite',title:'Private video invitation',body:'You have been invited to a private wellness conversation.',entity_id:data.id});alert('Video invitation sent safely.')}}
  const filtered=people.filter(p=>(p.display_name||p.full_name).toLowerCase().includes(query.toLowerCase())||(p.country||'').toLowerCase().includes(query.toLowerCase()))
  return <div className="feature-overlay" ref={containerRef}><section className="directory-window community-lobby" role="dialog" aria-modal="true" aria-label="Community lobby"><header><div><h2>Community Lobby</h2><p>Join peaceful public wellness rooms or connect one-to-one.</p></div><button onClick={onClose}><X/></button></header><div className="lobby-scroll"><div className="lobby-grid">{rooms.map(r=><article key={r.id} className={`room-card ${r.theme}`}><div className="room-art"><span>{r.icon}</span><i className="bubble b1"/><i className="bubble b2"/><i className="bubble b3"/></div><div className="room-info"><div className="tags"><span className="open-tag"><i/> Open</span>{(r.online_members||0)>0&&<span>LIVE</span>}{(r.tags||[]).slice(0,2).map(t=><span key={t}>{t}</span>)}</div><h3>{r.name}</h3><p>{r.description}</p><div className="lobby-stats"><span>{r.online_members||0} online</span><span>{r.total_members||0} members</span></div><div className="room-bottom"><span><MessageCircleMore/> Community room</span><button disabled={joining===r.id} onClick={()=>joinRoom(r)}>{joining===r.id?'Joining…':'Join Room'} <ChevronLeft style={{transform:'rotate(180deg)'}}/></button></div></div></article>)}</div><div className="member-section"><div><h3>Members and healers</h3><p>Find people for Connect requests and private two-person conversations.</p></div><input className="people-search" placeholder="Search by name or country…" value={query} onChange={e=>setQuery(e.target.value)} aria-label="Search by name or country"/><div className="people-list">{filtered.map(p=><article key={p.id}><span className="person-avatar">{p.avatar_url?<img src={p.avatar_url} alt={(p.display_name || p.full_name || 'Member') + " avatar"}/>:initials(p.display_name||p.full_name)}</span><div><h3>{p.display_name||p.full_name}{p.profile_type==='healer'&&<em>Healer / Therapist</em>}</h3><p>{p.country||'Nova Resort community'} · {p.about||'Here to connect and grow.'}</p></div><button title="Private message" onClick={()=>message(p)}><MessageCircleMore/></button><ConnectButton userId={userId} person={p} friendships={friendships} blocked={blocked} onChanged={load}/><button onClick={()=>video(p)}><Video/></button></article>)}</div></div></div></section></div>
}

export function Connections({userId,onClose,onOpenRoom}:{userId:string;onClose:()=>void;onOpenRoom:(room:DbRoom)=>void}) {
  const [tab,setTab]=useState<'connections'|'incoming'|'sent'|'suggested'|'blocked'>('connections'),[people,setPeople]=useState<Profile[]>([]),[blockedRows,setBlockedRows]=useState<{blocked_id:string;profiles?:Profile|null}[]>([]),[query,setQuery]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('')
  const containerRef = useFocusTrap(true)
  const {friendships,blocked,load:loadRelations}=useConnections(userId)
  const load=useCallback(async()=>{setLoading(true);setError('');const [{data:p,error:pe},{data:b,error:be}]=await Promise.all([supabase.from('profiles').select('id,full_name,display_name,avatar_url,country,profile_type,about,interests,specialties,online').neq('id',userId).limit(120),supabase.from('user_blocks').select('blocked_id,profiles!user_blocks_blocked_id_fkey(id,full_name,display_name,avatar_url,country,profile_type,about,interests,specialties,online)').eq('blocker_id',userId)]);if(pe||be){setError(pe?.message||be?.message||'Connections could not be loaded.');setLoading(false);return}setPeople((p as Profile[])||[]);setBlockedRows((b as any[])||[]);await loadRelations();setLoading(false)},[userId,loadRelations])
  useEffect(()=>{load()},[load])
  async function message(p:Profile){const {data,error}=await supabase.rpc('create_private_room',{other_user:p.id});if(error){alert(error.message);return}onClose();onOpenRoom({id:data,name:p.display_name||p.full_name,description:'Private two-person conversation',icon:'♢',theme:'sage',is_private:true})}
  async function block(p:Profile){if(!window.confirm(`Block ${p.display_name||p.full_name}?`))return;const {error}=await supabase.rpc('block_member',{other_user:p.id});if(error)alert(error.message);else load()}
  const byId=new Map(people.map(p=>[p.id,p])), match=(p:Profile)=>`${p.display_name||p.full_name} ${p.country||''} ${(p.interests||[]).join(' ')} ${p.profile_type}`.toLowerCase().includes(query.toLowerCase())
  const connected=friendships.filter(f=>f.status==='accepted').map(f=>byId.get(f.requester_id===userId?f.addressee_id:f.requester_id)).filter(Boolean) as Profile[]
  const incoming=friendships.filter(f=>f.status==='pending'&&f.addressee_id===userId).map(f=>byId.get(f.requester_id)).filter(Boolean) as Profile[]
  const sent=friendships.filter(f=>f.status==='pending'&&f.requester_id===userId).map(f=>byId.get(f.addressee_id)).filter(Boolean) as Profile[]
  const suggested=people.filter(p=>!connectionFor(p.id,friendships)&&!blocked.includes(p.id)).sort((a,b)=>(b.interests||[]).length-(a.interests||[]).length)
  const blockedProfiles=blockedRows.map(r=>r.profiles).filter(Boolean) as Profile[]
  const lists={connections:connected,incoming,sent,suggested,blocked:blockedProfiles},list=lists[tab].filter(match)
  const tabs:[typeof tab,string,number][]=[['connections','My connections',connected.length],['incoming','Incoming requests',incoming.length],['sent','Sent requests',sent.length],['suggested','Suggested connections',suggested.length],['blocked','Blocked users',blockedProfiles.length]]
  return <div className="feature-overlay" ref={containerRef}><section className="directory-window connections-window" role="dialog" aria-modal="true" aria-label="Connections"><header><div><h2>Connections</h2><p>Manage Connect requests, trusted relationships, and blocked users.</p></div><button onClick={onClose}><X/></button></header><div className="connection-tabs">{tabs.map(([id,label,count])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}>{label}<span>{count}</span></button>)}</div><input className="people-search" placeholder="Search by name, country, interests, or profile type…" value={query} onChange={e=>setQuery(e.target.value)} aria-label="Search connections"/>{loading?<div className="empty-state">Loading connections…</div>:error?<div className="empty-state"><MessageCircleMore/><h3>Connections unavailable</h3><p>{error}</p></div>:list.length===0?<div className="empty-state"><UsersRound/><h3>No results</h3><p>{tab==='suggested'?'Suggestions appear from real member profiles when available.':'Nothing in this list yet.'}</p></div>:<div className="people-list">{list.map(p=><article key={p.id}><span className="person-avatar">{p.avatar_url?<img src={p.avatar_url} alt={(p.display_name || p.full_name || 'Member') + " avatar"}/>:initials(p.display_name||p.full_name)}</span><div><h3>{p.display_name||p.full_name}{p.profile_type==='healer'&&<em>Healer / Therapist</em>}</h3><p>{p.country||'Nova Resort community'} · {(p.interests||[]).slice(0,3).join(', ')||p.about||'Open to meaningful wellness connection.'}</p></div>{tab!=='blocked'&&<button title="Private message" onClick={()=>message(p)}><MessageCircleMore/></button>}{tab!=='blocked'&&<ConnectButton userId={userId} person={p} friendships={friendships} blocked={blocked} onChanged={load}/>}<button onClick={()=>block(p)}>{tab==='blocked'?'Blocked':'Block'}</button></article>)}</div>}</section></div>
}

type PrivateRoom = DbRoom & { avatar_url:string|null;other_user_id?:string;other_online?:boolean;other_last_seen?:string;verified?:boolean;last_message:string|null;last_sender_id?:string|null;last_message_at?:string|null;last_activity:string;last_read_at?:string|null;unread_count?:number }
export function PrivateChats({onClose,onOpenRoom}:{onClose:()=>void;onOpenRoom:(room:DbRoom)=>void}){
  const [rooms,setRooms]=useState<PrivateRoom[]>([]),[loading,setLoading]=useState(true),[people,setPeople]=useState<Profile[]>([]),[creating,setCreating]=useState(false),[query,setQuery]=useState(''),[busy,setBusy]=useState(''),[error,setError]=useState('')
  const containerRef = useFocusTrap(true)
  const loadRooms=useCallback(()=>supabase.rpc('list_private_rooms').then(({data,error})=>{if(error)setError(error.message);else{setRooms((data as PrivateRoom[])||[]);setError('')}setLoading(false)}),[])
  useEffect(()=>{loadRooms();const c=supabase.channel('private-inbox').on('postgres_changes',{event:'*',schema:'public',table:'messages'},()=>loadRooms()).on('postgres_changes',{event:'*',schema:'public',table:'notifications'},()=>loadRooms()).on('postgres_changes',{event:'*',schema:'public',table:'room_user_preferences'},()=>loadRooms()).subscribe();return()=>{supabase.removeChannel(c)}},[loadRooms])
  useEffect(()=>{if(!creating)return;supabase.auth.getUser().then(({data})=>supabase.from('profiles').select('id,full_name,display_name,avatar_url,country,profile_type,about,interests,specialties,online').neq('id',data.user?.id||'').limit(80).then(({data})=>setPeople((data as Profile[])||[])))},[creating])
  async function createRoom(p:Profile){setBusy(p.id);const {data,error}=await supabase.rpc('create_private_room',{other_user:p.id});setBusy('');if(error){alert(error.message);return}onClose();onOpenRoom({id:data,name:p.display_name||p.full_name,description:'Private two-person conversation',icon:'♢',theme:'sage',is_private:true})}
  const q=query.toLowerCase(),filtered=people.filter(p=>(p.display_name||p.full_name).toLowerCase().includes(q)||(p.country||'').toLowerCase().includes(q)),roomResults=rooms.filter(r=>r.name.toLowerCase().includes(q)||(r.last_message||'').toLowerCase().includes(q))
  return <div className="feature-overlay" ref={containerRef}><section className="directory-window private-chats" role="dialog" aria-modal="true" aria-label="Private messages"><header><div><h2>{creating?'Create private room':'Messages'}</h2><p>{creating?'Choose exactly one person. Only both of you can open this room.':'Recent private conversations, unread messages, and new rooms.'}</p></div><button style={{width:'auto',height:34,marginLeft:'auto',padding:'0 12px',whiteSpace:'nowrap'}} onClick={()=>setCreating(!creating)}>{creating?'View chats':'New message'}</button><button onClick={onClose}><X/></button></header><input className="people-search" placeholder={creating?'Search for the second person…':'Search conversations…'} value={query} onChange={e=>setQuery(e.target.value)} aria-label="Search private messages"/>{creating?<div className="people-list">{filtered.length===0?<div className="empty-state"><MessageCircleMore/><h3>No people found</h3><p>Try searching by name or country.</p></div>:filtered.map(p=><article key={p.id}><span className="person-avatar">{p.avatar_url?<img src={p.avatar_url} alt={(p.display_name || p.full_name || 'Member') + " avatar"}/>:initials(p.display_name||p.full_name)}<i className={p.online?'online':''}/></span><div><h3>{p.display_name||p.full_name}{p.profile_type==='healer'&&<em>Healer / Therapist</em>}</h3><p>{p.country||'Nova Resort community'} · Private room for 2 users only</p></div><button disabled={busy===p.id} onClick={()=>createRoom(p)}>{busy===p.id?'…':<MessageCircleMore/>}</button></article>)}</div>:loading?<div className="empty-state">Loading conversations…</div>:error?<div className="empty-state"><MessageCircleMore/><h3>Messages unavailable</h3><p>{error}</p></div>:rooms.length===0?<div className="empty-state"><MessageCircleMore/><h3>No private conversations yet</h3><p>Create a private room with one other member.</p><button className="save-profile" onClick={()=>setCreating(true)}>Create private room</button></div>:<div className="people-list private-inbox-list">{roomResults.map(r=><button className={r.unread_count?'private-chat-row unread':'private-chat-row'} key={r.id} onClick={()=>{onClose();onOpenRoom(r)}}><span className="person-avatar">{r.avatar_url?<img src={r.avatar_url} alt={r.name + " avatar"}/>:r.name.slice(0,1)}<i className={r.other_online?'online':''}/></span><div><h3>{r.name}{r.verified&&<em>Verified healer</em>}</h3><p>{r.last_sender_id?`${r.last_sender_id===r.other_user_id?r.name:'You'}: `:''}{r.last_message||'Start your private conversation.'}</p>{r.other_online&&<small>Online now</small>}</div><time>{r.last_activity&&new Date(r.last_activity).toLocaleDateString()}</time>{Boolean(r.unread_count)&&<b>{r.unread_count}</b>}</button>)}</div>}</section></div>
}

export function EditProfile({userId,onClose}:{userId:string;onClose:()=>void}){
  const [profile,setProfile]=useState<Profile|null>(null),[email,setEmail]=useState<string|null>(null),[saved,setSaved]=useState(false),[uploading,setUploading]=useState(false),[error,setError]=useState(''),[signingOut,setSigningOut]=useState(false)
  const containerRef = useFocusTrap(true)
  useEffect(()=>{supabase.from('profiles').select('*').eq('id',userId).single().then(({data})=>setProfile(data as Profile));supabase.auth.getUser().then(({data})=>setEmail(data.user?.email||null))},[userId])
  async function save(e:FormEvent<HTMLFormElement>){
    e.preventDefault();setError('');setSaved(false)
    const d=new FormData(e.currentTarget)
    const birthDate=String(d.get('birth_date')||'')
    if (birthDate && new Date(birthDate) > new Date()) { setError('Date of birth cannot be in the future.'); return }
    const payload:Record<string,unknown>={
      display_name:String(d.get('display_name')||'').trim(),
      country:String(d.get('country')||'').trim()||null,
      city:String(d.get('city')||'').trim()||null,
      about:String(d.get('about')||'').trim().slice(0,500),
      interests:splitList(d.get('interests')),
      languages:splitList(d.get('languages')),
      birth_date:birthDate||null,
      birth_date_visibility:String(d.get('birth_date_visibility')||'private'),
      pronouns:String(d.get('pronouns')||'').trim()||null,
      personal_website:safeUrl(d.get('personal_website')),
      visibility:String(d.get('visibility')||'community'),
      discoverable:d.get('discoverable') === 'on',
      updated_at:new Date().toISOString()
    }
    if (isApprovedHealer(profile)) {
      Object.assign(payload,{
        professional_title:String(d.get('professional_title')||'').trim()||null,
        specialties:splitList(d.get('specialties')),
        availability:String(d.get('availability')||'').trim()||null,
        years_experience:Number(d.get('years_experience')||0) || null,
        professional_website:safeUrl(d.get('professional_website')),
        linkedin_url:safeUrl(d.get('linkedin_url')),
        location:String(d.get('location')||'').trim()||null,
        certifications:splitList(d.get('certifications')),
        session_price:Number(d.get('session_price')||0) || 0,
        online_available:d.get('online_available') === 'on',
        in_person_available:d.get('in_person_available') === 'on',
        social_instagram_url:safeUrl(d.get('social_instagram_url')),
        social_facebook_url:safeUrl(d.get('social_facebook_url')),
        social_youtube_url:safeUrl(d.get('social_youtube_url'))
      })
    }
    const {error}=await supabase.from('profiles').update(payload).eq('id',userId)
    if(error){setError(error.message);return}
    setProfile(p=>p?{...p,...payload} as Profile:p);setSaved(true)
  }
  async function signOut(){setSigningOut(true);setError('');const {error}=await supabase.auth.signOut();if(error){setError('We could not sign you out. Please try again.');setSigningOut(false)}}
  async function upload(file?:File){if(!file)return;if(!['image/jpeg','image/png','image/webp','image/gif'].includes(file.type)){setError('Please choose a JPG, PNG, WebP, or GIF image.');return}if(file.size>5242880){setError('Please choose an image smaller than 5 MB.');return}setUploading(true);setError('');const ext=file.name.split('.').pop()?.toLowerCase()||'jpg',path=`${userId}/profile.${ext}`;const {error}=await supabase.storage.from('avatars').upload(path,file,{upsert:true,contentType:file.type});if(!error){const {data}=supabase.storage.from('avatars').getPublicUrl(path);const avatar_url=`${data.publicUrl}?v=${Date.now()}`;await supabase.from('profiles').update({avatar_url,updated_at:new Date().toISOString()}).eq('id',userId);setProfile(p=>p?{...p,avatar_url}:p)}else setError(error.message);setUploading(false)}
  const approved=isApprovedHealer(profile)
  return <div className="feature-overlay" ref={containerRef}><section className="profile-window profile-editor-window" role="dialog" aria-modal="true" aria-label="Profile editor"><header><div><h2>Your profile</h2><p>Help the right people connect with you.</p></div><button onClick={onClose}><X/></button></header>{profile&&<form onSubmit={save}>{error&&<div className="form-message error">{error}</div>}<section className="profile-section"><h3>Personal Information</h3><div className="profile-photo-editor"><span>{profile.avatar_url?<img src={profile.avatar_url} alt="Profile"/>:initials(profile.display_name||profile.full_name)}</span><label><Camera/>{uploading?'Uploading…':'Add profile picture'}<input type="file" accept="image/jpeg,image/png,image/webp,image/gif" onChange={e=>upload(e.target.files?.[0])}/></label><small>JPG, PNG, WebP or GIF · Max 5 MB</small></div><label>Display name<input name="display_name" required defaultValue={profile.display_name||profile.full_name}/></label><div className="form-row"><label>Pronouns<input name="pronouns" defaultValue={profile.pronouns||''} placeholder="Optional"/></label><label>Account type<input readOnly value={accountTypeLabel(profile.profile_type)} aria-readonly="true"/></label></div></section><section className="profile-section"><h3>About Me</h3><label>Short biography<textarea name="about" maxLength={500} defaultValue={profile.about} placeholder="Share a little about yourself…"/></label><label>Interests <small>Separate with commas</small><input name="interests" defaultValue={(profile.interests||[]).join(', ')}/></label><label>Languages <small>Separate with commas</small><input name="languages" defaultValue={(profile.languages||[]).join(', ')}/></label></section><section className="profile-section"><h3>Location and Privacy</h3><div className="form-row"><label>Country<input name="country" defaultValue={profile.country||''}/></label><label>City<input name="city" defaultValue={profile.city||''}/></label></div><div className="form-row"><label>Date of birth<input type="date" name="birth_date" defaultValue={profile.birth_date||''}/></label><label>Birthday privacy<select name="birth_date_visibility" defaultValue={profile.birth_date_visibility||'private'}><option value="private">Private</option><option value="age_range">Show age range only</option><option value="birthday_only">Show birthday without year</option></select></label></div><label>Personal website or social link<input type="url" name="personal_website" defaultValue={profile.personal_website||''} placeholder="https://"/></label><div className="form-row"><label>Profile visibility<select name="visibility" defaultValue={profile.visibility||'community'}><option value="community">Community</option><option value="friends">Friends only</option><option value="private">Private</option></select></label><label className="check-label"><input type="checkbox" name="discoverable" defaultChecked={profile.discoverable!==false}/>Show me in member discovery</label></div></section>{approved&&<section className="profile-section professional-profile-section"><h3><ShieldCheck size={16}/> Professional Healer Profile</h3><label>Professional title<input name="professional_title" defaultValue={profile.professional_title||''} placeholder="Therapist, Life Coach, Mindfulness Teacher…"/></label><label>Areas of Treatment and Expertise <small>Separate with commas</small><input name="specialties" defaultValue={(profile.specialties||[]).join(', ')}/></label><label>Therapy methods and session information<textarea name="availability" defaultValue={profile.availability||''} placeholder="CBT, mindfulness-based therapy, online sessions, workshops, booking availability…"/></label><div className="form-row"><label>Years of professional experience<input type="number" min={0} name="years_experience" defaultValue={profile.years_experience||''}/></label><label>Location (City, Country)<input name="location" defaultValue={(profile as any).location||''} placeholder="e.g., Tel Aviv, Israel"/></label></div><div className="form-row"><label>Certifications <small>Separate with commas</small><input name="certifications" defaultValue={((profile as any).certifications||[]).join(', ')} placeholder="e.g., Licensed Clinical Social Worker"/></label><label>Session price (USD)<input type="number" min={0} name="session_price" defaultValue={(profile as any).session_price||0} placeholder="0 for free"/></label></div><div className="form-row"><label className="check-label"><input type="checkbox" name="online_available" defaultChecked={(profile as any).online_available!==false}/>Available online</label><label className="check-label"><input type="checkbox" name="in_person_available" defaultChecked={(profile as any).in_person_available===true}/>Available in-person</label></div><label>Professional website<input type="url" name="professional_website" defaultValue={profile.professional_website||''} placeholder="https://"/></label><label>LinkedIn profile<input type="url" name="linkedin_url" defaultValue={profile.linkedin_url||''} placeholder="https://linkedin.com/in/..."/></label><label>Instagram URL<input type="url" name="social_instagram_url" defaultValue={(profile as any).social_instagram_url||''} placeholder="https://instagram.com/..."/></label><div className="form-row"><label>Facebook URL<input type="url" name="social_facebook_url" defaultValue={(profile as any).social_facebook_url||''} placeholder="https://facebook.com/..."/></label><label>YouTube URL<input type="url" name="social_youtube_url" defaultValue={(profile as any).social_youtube_url||''} placeholder="https://youtube.com/..."/></label></div></section>}<section className="profile-section account-section"><h3>Account</h3><button type="button" className="signout-inline" disabled={signingOut} onClick={signOut}><LogOut size={16}/>{signingOut?'Signing out…':'Sign Out'}</button></section><button className="save-profile">{saved?'Saved ✓':'Save profile'}</button></form>}</section></div>
}

export function Notifications({userId,onClose}:{userId:string;onClose:()=>void}){
  const [items,setItems]=useState<Notice[]>([])
  const containerRef = useFocusTrap(true)
  const load=()=>supabase.from('notifications').select('*').eq('user_id',userId).order('created_at',{ascending:false}).limit(50).then(({data})=>setItems((data as Notice[])||[]))
  useEffect(()=>{load();const c=supabase.channel(`notices-${userId}`).on('postgres_changes',{event:'*',schema:'public',table:'notifications',filter:`user_id=eq.${userId}`},()=>load()).subscribe();return()=>{supabase.removeChannel(c)}},[userId])
  const unread=items.filter(n=>!n.read_at)
  async function markRead(n:Notice){if(!n.read_at){await supabase.from('notifications').update({read_at:new Date().toISOString()}).eq('id',n.id);window.dispatchEvent(new CustomEvent('nova-notifications-read'));load()}}
  async function markAllRead(){if(unread.length===0)return;await supabase.from('notifications').update({read_at:new Date().toISOString()}).eq('user_id',userId).is('read_at',null);window.dispatchEvent(new CustomEvent('nova-notifications-read'));load()}
  async function dismiss(n:Notice){await supabase.from('notifications').delete().eq('id',n.id);window.dispatchEvent(new CustomEvent('nova-notifications-read'));load()}
  async function clearRead(){const readIds=items.filter(n=>n.read_at).map(n=>n.id);if(readIds.length===0)return;await supabase.from('notifications').delete().in('id',readIds);load()}
  async function respond(n:Notice,next_status:'accepted'|'declined'){if(!n.entity_id)return;const {error}=await supabase.rpc('respond_connection_request',{request_id:n.entity_id,next_status});if(error)alert(error.message);else await markRead(n)}
  async function openVideo(n:Notice){await markRead(n);if(n.entity_id){await supabase.from('video_sessions').update({status:'active',started_at:new Date().toISOString()}).eq('id',n.entity_id);window.open(`https://meet.jit.si/NovaResort-${n.entity_id}`,'_blank','noopener,noreferrer')}load()}
  const readItems=items.filter(n=>n.read_at)
  return <div className="feature-overlay" ref={containerRef}><section className="notification-window" role="dialog" aria-modal="true" aria-label="Notifications"><header><div><h2>Notifications</h2><p>{unread.length>0?`${unread.length} unread`:'All caught up'}</p></div><div className="notification-header-actions">{unread.length>0&&<button className="notif-action" onClick={markAllRead}><CheckCheck size={14}/> Mark all read</button>}{readItems.length>0&&<button className="notif-action" onClick={clearRead}><Trash2 size={14}/> Clear read</button>}<button className="notif-action" onClick={()=>{onClose();setTimeout(()=>{window.location.hash='#/notif-prefs'},50)}}><Settings size={14}/> Preferences</button><button onClick={onClose}><X/></button></div></header>{items.length===0?<div className="empty-state"><Bell/><h3>You're all caught up</h3><p>No new notifications.</p></div>:items.map(n=><article className={n.read_at?'notice read':'notice'} key={n.id}><span>{n.type==='video_invite'?<Video/>:<Heart/>}</span><div><b>{n.title}</b><p>{n.body}</p><small>{new Date(n.created_at).toLocaleString()}</small>{n.type==='connection_request'&&n.entity_id?<div className="notice-actions"><button onClick={()=>respond(n,'accepted')}><Check size={14}/> Accept</button><button onClick={()=>respond(n,'declined')}>Deny</button></div>:n.type==='video_invite'?<div className="notice-actions"><button onClick={()=>openVideo(n)}><Video size={14}/> Open invite</button></div>:null}</div><button className="notif-dismiss" onClick={()=>dismiss(n)} title="Dismiss"><X size={14}/></button></article>)}</section></div>
}

export function SafetyCenter({onClose}:{onClose:()=>void}){
  const containerRef = useFocusTrap(true)
  return <div className="feature-overlay" ref={containerRef}><section className="profile-window safety-window" role="dialog" aria-modal="true" aria-label="Safety center"><header><div><h2>Community safety</h2><p>A warm community depends on clear, caring boundaries.</p></div><button onClick={onClose}><X/></button></header><div className="safety-content"><Heart/><h3>Respect every person</h3><p>No harassment, bullying, hate speech, discrimination, explicit content or spam.</p><h3>Protect privacy</h3><p>Keep private conversations confidential. Never share another member’s personal information.</p><h3>Wellness support, not emergency care</h3><p>Members and healers must not provide an unsupported medical diagnosis. If someone is in immediate danger, contact local emergency services.</p><h3>Report harmful behaviour</h3><p>Block or report anyone who makes you feel unsafe. Serious violations may lead to suspension.</p><button className="save-profile" onClick={onClose}>I understand</button></div></section></div>
}
